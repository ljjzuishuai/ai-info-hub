# ===== Railway 环境变量配置 =====
# 在 Railway Dashboard → Variables 中添加以下变量：

# 服务端口（Railway 自动设置，本地用3001）
PORT=3001

# JWT签名密钥（必填！请生成随机字符串替换，至少32字符）
JWT_SECRET=请替换为随机生成的32位以上字符串

# 运行环境（部署时设置为 production）
NODE_ENV=production

# 前端地址（Railway 会自动分配域名，部署后更新）
CLIENT_ORIGIN=https://your-app.railway.app
